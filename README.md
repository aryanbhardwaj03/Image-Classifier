# Image-Classifier
A deep learning image classification project built using Convolutional Neural Networks (CNN) and TensorFlow/Keras. Includes data preprocessing, model training, evaluation, and prediction on custom images with visualization of results.
